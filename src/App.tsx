import React from 'react';
import Canvas from '@/pages/Canvas';

const App: React.FC = () => {
  return (
    <div className="h-screen w-screen">
      <Canvas />
    </div>
  );
};

export default App;
