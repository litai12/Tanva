
# 资源代理：用于绕过 OSS 跨域限制（视频抽帧/缩略图 capture 等需要）
VITE_PROXY_ASSETS=false

# 直连 OSS/CDN 的公共 base（用于将 key 拼成可访问 URL；例如 https://cdn.example.com）
# 当 VITE_PROXY_ASSETS=false 且项目内容里大量使用 `projects/...` 这类 key 时，务必配置此项，否则仍会回退走 `/api/assets/proxy?key=...`。
VITE_ASSET_PUBLIC_BASE_URL=

#后端 API 地址
VITE_API_BASE_URL=http://localhost:4000

# AI Chat 并行图片生成并发上限（1-10）
VITE_AI_IMAGE_PARALLEL_CONCURRENCY=10
