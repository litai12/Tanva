# Semi UI (@douyinfe/semi-ui) resolves its SCSS theme via webpack-style `~` imports
# and per-component `.css` files that require a flat node_modules layout. pnpm's
# default isolated/symlinked store breaks that resolution, so use a hoisted layout.
node-linker=hoisted
