--- a/apps/hono-api/src/modules/task/agents-host-identity.ts
+++ b/apps/hono-api/src/modules/task/agents-host-identity.ts
@@ -0,0 +1,5 @@
+/** Use the same tenant boundary for execution, status and terminal projection. */
+export function resolveAgentsHostUserId(ownerId: string, hostUserId?: string | null): string {
+  const host = (hostUserId || "").trim().replace(/[^a-zA-Z0-9._-]/g, "_").slice(0, 120);
+  return ownerId && host ? `${ownerId}:${host}` : ownerId;
+}
--- a/apps/hono-api/src/modules/task/async-agent-continuation.ts
+++ b/apps/hono-api/src/modules/task/async-agent-continuation.ts
@@ -1,3 +1,4 @@
+import { resolveAgentsHostUserId } from "./agents-host-identity";
 import { createHash, randomUUID } from "node:crypto";
 import type { AppContext } from "../../types";
 import { getFlowByIdUnsafe, mapFlowRowToDto } from "../flow/flow.repo";
@@ -411,9 +412,11 @@
 	reasonCode?: "async_dependency_terminal";
 }): Promise<void> {
 	const rootRequestId = input.continuation.rootRequestId?.trim() ?? "";
-	if (!rootRequestId.startsWith("public-chat-turn:")) return;
+	if (!rootRequestId) return;
 	try {
-		const receipt = await interruptAgentsChatTurn(input.c, input.continuation.userId, {
+		const receipt = await interruptAgentsChatTurn(input.c, resolveAgentsHostUserId(
+			input.continuation.userId, input.continuation.hostUserId,
+		), {
 			sessionId: input.continuation.sessionKey,
 			turnId: rootRequestId,
 			reasonCode: input.reasonCode ?? "async_dependency_terminal",
--- a/apps/hono-api/src/modules/task/logical-task-budget.ts
+++ b/apps/hono-api/src/modules/task/logical-task-budget.ts
@@ -4,10 +4,11 @@
 /** Admission timestamps are durable facts; physical retries cannot mint a new target. */
 export async function resolveLogicalTaskBudget(input: {
   traceId: string;
+  admissionTraceId?: string;
   readTrace: (traceId: string) => Promise<ExecutionTraceLifecycleSnapshot | null>;
 }): Promise<LogicalTaskBudget> {
   const seen = new Set<string>();
-  let traceId = input.traceId;
+  let traceId = input.traceId.trim() || input.admissionTraceId?.trim() || "";
   while (!seen.has(traceId)) {
     seen.add(traceId);
     const trace = await input.readTrace(traceId);
--- a/apps/hono-api/src/modules/task/public-chat-host-async-evidence.ts
+++ b/apps/hono-api/src/modules/task/public-chat-host-async-evidence.ts
@@ -56,20 +56,25 @@
 }): PublicChatHostExecutionHandoffEvidenceV1 | null {
 	if (!input.manifest) return null;
 	const allowedOps = new Set<string>(input.manifest.patchOps);
+	const allowedHostTools = new Set((input.manifest.hostTools ?? []).map((tool) => tool.name));
 	const commands = input.toolCalls.filter((toolCall) => {
 		if (
-			toolCall.name !== "flow_patch" ||
 			toolCall.status !== "succeeded" ||
 			!toolCall.toolCallId ||
 			!toolCall.inputJson
 		) return false;
+		if (toolCall.name === "host_tool") {
+			const name = readRequiredString(toolCall.inputJson.name);
+			return Boolean(name && allowedHostTools.has(name));
+		}
+		if (toolCall.name !== "flow_patch") return false;
 		const op = readRequiredString(toolCall.inputJson.op);
 		return Boolean(op && allowedOps.has(op));
 	});
 	const runNodeCount = commands.filter(
 		(toolCall) => toolCall.inputJson?.op === "runNode",
 	).length;
-	if (commands.length === 0 || runNodeCount === 0) return null;
+	if (commands.length === 0) return null;
 	return {
 		version: 1,
 		owner: "external_host",
@@ -113,7 +118,7 @@
 		? evidence.commandCount
 		: null;
 	const runNodeCount = typeof evidence?.runNodeCount === "number" &&
-		Number.isInteger(evidence.runNodeCount) && evidence.runNodeCount > 0
+		Number.isInteger(evidence.runNodeCount) && evidence.runNodeCount >= 0
 		? evidence.runNodeCount
 		: null;
 	if (
@@ -134,6 +139,7 @@
 		taskRevision === null ||
 		commandCount === null ||
 		runNodeCount === null ||
+		runNodeCount > commandCount ||
 		commandToolCallIds.length !== commandCount ||
 		ticket.logicalTaskId !== logicalTaskId ||
 		ticket.taskNodeId !== taskNodeId ||
--- a/apps/hono-api/src/modules/task/public-openai-logical-turn.ts
+++ b/apps/hono-api/src/modules/task/public-openai-logical-turn.ts
@@ -1,3 +1,4 @@
+import { resolveAgentsHostUserId } from "./agents-host-identity";
 import { setTimeout as delay } from "node:timers/promises";
 import { AppError } from "../../middleware/error";
 import type { AppContext } from "../../types";
@@ -112,13 +113,14 @@
 export function waitForPersistedOpenAiLogicalTurn(input: Readonly<{
 	c: AppContext;
 	userId: string;
+	hostUserId?: string;
 	sessionKey: string;
 	rootRequestId: string;
 	abortSignal?: AbortSignal;
 }>): Promise<OpenAiLogicalTurnTerminal> {
 	return waitForOpenAiLogicalTurnTerminal({
 		rootRequestId: input.rootRequestId,
-		readStatus: () => getAgentsChatTurnStatus(input.c, input.userId, input.sessionKey, {
+		readStatus: () => getAgentsChatTurnStatus(input.c, resolveAgentsHostUserId(input.userId, input.hostUserId), input.sessionKey, {
 			timeoutMs: OPENAI_LOGICAL_TURN_STATUS_TIMEOUT_MS,
 			...(input.abortSignal ? { signal: input.abortSignal } : {}),
 		}),
--- a/apps/hono-api/src/modules/task/public-openai-compat.ts
+++ b/apps/hono-api/src/modules/task/public-openai-compat.ts
@@ -261,7 +261,8 @@
 		registration.commandCount > 0 &&
 		typeof registration.runNodeCount === "number" &&
 		Number.isInteger(registration.runNodeCount) &&
-		registration.runNodeCount > 0;
+		registration.runNodeCount >= 0 &&
+		registration.runNodeCount <= registration.commandCount;
 }
 
 export function isOpenAiDurableContinuationPending(input: {
@@ -583,6 +584,7 @@
 				const terminal = await waitForPersistedOpenAiLogicalTurn({
 					c,
 					userId,
+					hostUserId,
 					sessionKey: executionSessionKey,
 					rootRequestId: requestId,
 					...(inflight ? { abortSignal: inflight.signal } : {}),
@@ -877,6 +879,7 @@
 				const terminal = await waitForPersistedOpenAiLogicalTurn({
 					c,
 					userId,
+					hostUserId,
 					sessionKey: executionSessionKey,
 					rootRequestId: requestId,
 					...(inflight ? { abortSignal: inflight.signal } : {}),
--- a/apps/hono-api/src/modules/task/task.agents-bridge.ts
+++ b/apps/hono-api/src/modules/task/task.agents-bridge.ts
@@ -1,3 +1,4 @@
+import { resolveAgentsHostUserId } from "./agents-host-identity";
 import { resolveLogicalTaskBudget } from "./logical-task-budget";
 import { getExecutionTraceLifecycleSnapshot } from "../memory/execution-trace-events.repo";
 import { assertChatModelAllowed } from "../new-api-models/chat-model-policy";
@@ -8788,10 +8789,7 @@
 		typeof extras.hostUserId === "string"
 			? extras.hostUserId.trim()
 			: "";
-	const effectiveUserId =
-		baseEffectiveUserId && hostUserIdRaw
-			? `${baseEffectiveUserId}:${sanitizePathSegmentForAgents(hostUserIdRaw)}`
-			: baseEffectiveUserId;
+	const effectiveUserId = resolveAgentsHostUserId(baseEffectiveUserId, hostUserIdRaw);
 	if (!effectiveUserId) {
 		throw new AppError("Unauthorized: missing userId for agents bridge", {
 			status: 401,
@@ -9808,8 +9806,9 @@
 	const budgetRootId = options?.trustedInternalExecution === true
 		&& typeof budgetRootIdRaw === "string" && budgetRootIdRaw.trim()
 		? budgetRootIdRaw.trim() : publicTurnId;
-	const logicalTaskBudget = budgetRootId ? await resolveLogicalTaskBudget({
+	const logicalTaskBudget = budgetRootId || publicAgentsRequest ? await resolveLogicalTaskBudget({
 		traceId: budgetRootId,
+		admissionTraceId: requestId,
 		readTrace: (traceId) => getExecutionTraceLifecycleSnapshot(c.env.DB, { traceId, userId: baseEffectiveUserId }),
 	}) : undefined;
 	const timeoutMs = readTimeoutFromRequestExtras(request) ?? readAgentsBridgeTimeoutMs(c);
--- a/apps/hono-api/src/modules/task/public-agents-chat.ts
+++ b/apps/hono-api/src/modules/task/public-agents-chat.ts
@@ -1940,7 +1940,7 @@
  * still incomplete and this turn introduced a previously unseen async asset
  * identity. Prompt text, node labels and skill names never participate.
  */
-async function registerPendingAsyncContinuation(input: {
+export async function registerPendingAsyncContinuation(input: {
 	c: AppContext;
 	userId: string;
 	rootRequestId: string | null;
@@ -1957,7 +1957,7 @@
 	if (
 		!input.parentContinuation &&
 		hostExecutionHandoff &&
-		shouldRegisterPhysicalContinuation({ meta })
+		readRecord(meta?.logicalTaskState)?.status === "waiting_external"
 	) {
 		return {
 			status: "external_handoff",
--- a/apps/hono-api/src/modules/apiKey/apiKey.schemas.ts
+++ b/apps/hono-api/src/modules/apiKey/apiKey.schemas.ts
@@ -381,7 +381,7 @@
 	ticketId: z.string().min(1).optional(),
 	host: z.string().min(1).optional(),
 	commandCount: z.number().int().positive().optional(),
-	runNodeCount: z.number().int().positive().optional(),
+	runNodeCount: z.number().int().nonnegative().optional(),
 	details: z.string().min(1).optional(),
 });
 
