# 2026-04-09 Membership Ops And Display Checklist

- [x] 明确本次范围：不新增验证包产品，仅补订阅状态/管理动作/用户展示
- [x] 补 `task/` 执行清单并开始实现
- [x] 新增“订阅待生效变更”持久化模型
- [x] 会员接口返回 `nextChange`，支持展示当前套餐与下周期套餐
- [x] 新增管理员会员快捷操作接口
- [x] 用户中心展示当前套餐、到期时间、下周期变更
- [x] 会员面板按“订阅套餐 / 体验包”分类展示并补说明
- [x] 用户自助动作改为显式判定：开通 / 续费 / 立即升级 / 下周期降级
- [x] 升级订单支持按剩余周期折算补差价，并在支付成功后立即切换权限与补发积分
- [x] 降级走下周期生效，不创建支付订单
- [x] 后台补“验证完整性按钮”：切换预览、待生效切换、到期扫描、每日赠送、赠送衰减、年费刷新
- [x] 运行最小化验证并回填结果

## Verification

- `cd backend && npx prisma generate`
- `cd backend && npm run build`
- `cd frontend && npm run build`
