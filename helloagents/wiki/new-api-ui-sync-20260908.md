# new-api UI 同步与绘图日志缩略图（2026-09-08）

## 同步范围

来源 `/Users/libiqiang/workspace/TapCanvas-pro/apps/new-api/web`。迁入全局明暗主题样式、顶部导航样式、首页 Hero 和布局、动态 API 文档页及绘图日志缩略图。保留 Tanva 已有账户、渠道、计价与日志 API 合同，不引入依赖上游账户池/协议重构的管理入口。

## Tanvas 品牌

- `public/logo.png`、`logo.svg`、favicon 文件保持原样，未复制来源项目品牌资源。
- 默认与历史 `tanvasMart` 页面名称统一展示为 Tanvas；保留站点自定义名称和 logo 配置能力。
- HTML 标题、描述及默认页脚品牌使用 Tanvas；项目版权、许可证和 QuantumNous 链接保留。

## 页面适配

- 首页保留本地 `/api/stats/public` 数组合同与真实统计；不加载来源独有的模型时序统计接口，也不以缺失的注册/支付字段作暂停服务判断。
- 文档新增 `/docs`，兼容旧 `/console/docs`。读取本地 `/api/pricing` 和现有 OpenAPI 文件，生成模型/规格请求示例，支持复制当前 curl 和全文。
- 文档的可取消请求独立发送，避免开发严格模式重挂载时复用已 abort 的去重请求；页内统一显示错误。
- 文档代码颜色随明暗主题变化，修复来源浅色模式低对比度。

## 绘图日志

- 结果图片和参考图直接显示 96 × 80 缩略图，懒加载，等比完整展示；点击沿用大图预览。
- 无结果地址与加载失败分别提示。图片/参考图列原有默认可见设置保留。
- 历史 data:image/base64 输入先转换为 Blob URL 展示，组件清理时释放；原始日志字段不修改、不回写。仅支持远程 HTTP(S)、站内绝对路径及运行时 Blob URL，拒绝可执行协议。

## 验证与边界

- 11 项文档数据与图片引用测试通过。
- 生产前端构建通过；存在原有大 chunk / semi-ui 与 i18n 循环分块警告。
- Browser 实测首页与文档页、Tanvas 标识、主题切换；验收时文档读取 109 个模型、854 个规格，数量依当前网关配置变化。
- 使用真实缩略图组件和本地测试资源验证直接显示、失败提示与点击放大；没有登录管理账号读取真实绘图日志。
- logo/favicon 原始文件差异检查通过。
- 构建验证产物输出到 `/tmp/tanva-new-api-ui-20260908-dist`，仓库已跟踪的旧 dist 恢复为任务前版本，发布时需要重新构建 web 并打包 Go 服务。
- 未部署、未变更生产配置或数据库、未执行付费生成。
