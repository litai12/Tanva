# Director Console / LibTV 1:1 功能矩阵

> 唯一完成标准：Tanva Director Console 节点从画布外壳、打开体验、全部内部功能到输入/输出与保存重载，均有 LibTV 在线证据和本地浏览器回归证据。仅有按钮、仅有字段或仅通过构建不算完成。

## 节点外壳与 I/O

| 功能 | LibTV | Tanva 当前状态 | 验收证据 |
|---|---|---|---|
| 节点标题/说明 | 导演台；3D 搭景、多视角截图 | 已对齐主要文案 | 本地 snapshot |
| 资产管理 | 节点内与“打开导演台”并列 | 已补齐并真实打开 Tanva 统一素材资产面板（个人/团队资产及画布元素），不是空按钮 | 构建通过；真实画布点击/选材回归待补 |
| 输入句柄 | `target`，图片作为全景输入 | 已实现并将 handle 类型从宽泛 `any` 收紧为 `image`，阻止不支持的视频/音频连入 | DOM handle；连接刷新证据待补 |
| 输出句柄 | `source`，输出摄像机截图 | 已实现 | DOM handle |
| 当前裁剪输入 | 使用图片节点当前渲染/裁剪结果 | 已实现普通 Image 与 ImageSplit | 仍需保存重载验证 |
| 截图输出 | 远程图片节点并自动连边 | 已真实创建远程 JPEG、image 节点和边 | `img_1784381763834` 浏览器证据 |
| 保存/重载 | 场景、输入和输出边重载后保持 | 新建节点创建时已直接 seed 完整默认 scene；历史无 scene 节点首次打开会立即写回自愈，即使不编辑直接关闭也不再丢默认场景 | 输入/输出边真实项目统一刷新证据仍待补 |

## 打开后的工作区

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 顶栏：3D导演台、导演/机位视角、帮助、关闭 | 已实现 | 像素级尺寸/间距复核 |
| 左场景树：搜索、对象、隐藏、锁定 | 已实现；补齐常驻“搜索场景对象”标签、输入框、对象行 button 语义及 Enter/Space 选择 | 高斯/空对象/参考层的完整类型展示 |
| 新建时直接显示、点视口空白或按 Esc 进入 `3D场景` 控制器 | 已实现 | 旧节点会保留上次选中对象；浏览器回归待稳定项目 |
| 重置视角 | 已实现 | 与 LibTV 默认相机矩阵逐值对比 |
| 全屏 | 已实现浏览器 Fullscreen API | 需验证退出行为和工具条状态 |

## 3D 场景与环境

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 场景缩放/平移/旋转 | 已进入 WebGL 和截图；缩放显示 `300%`，XYZ 轴标签为可左右拖动按钮并保留精确输入 | 与 LibTV 变换对象范围精确对比；浏览器拖动手感待并排验证 |
| 全景背景连接状态 | 已实现输入连接与场景内远程全景两种状态 | 当前输入重载验证 |
| 天空颜色、全景 yaw、球半径 | 已实现；天空颜色对齐 `#` + 六位 hex 文本，全景旋转显示 `0°` | 非 2:1 图映射像素对比 |
| 角色标签 | 已实现并进入截图 | 字体/描边/位置像素对比 |
| 网格吸附 | 角色/机位 gizmo 松手按 0.5 X/Z 吸附 | LibTV 吸附步长仍需实测 |
| 高斯地面吸附 | 已解析标准 `.splat` XYZ、应用高斯对象变换、建立 0.25 场景单位 XZ 高度索引，并在角色/道具 gizmo 提交和键盘平移时写回点云表面 Y；关闭开关时不吸附 | 真实 CC0 32-byte `.splat` 已由 HTTP 加载并由 Drei 渲染，4225 splats/135200 bytes、控制台 0 error；同一远程解析索引在 X=-2/0/2 得到 0.098/0.296/0.472。仍需完整项目拖动与保存重载证据 |
| 地面显示/透明度/高度 | 已实现地面板+网格并进入截图；数值格式对齐 `0.40` / `0.0` | 材质颜色、网格尺度像素对比 |

## 添加对象

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 本地 GLB/GLTF | 先上传远程 URL 再添加 | GLTF 外部依赖文件打包未实现 |
| `.splat` 高斯泼溅 | 上传与 Drei Splat 渲染已接通 | 缺真实资产端到端回归、地面求交 |
| 八套素体 | 八项均已使用独立 Quaternius CC0 蒙皮模型：Universal Base 的 Superhero Male/Female，加 Ultimate Animated Character Pack 的 Viking、Knight、Ninja、Elf、Goblin Male/Female，分别映射宽厚、健壮、纤细、少年、儿童、二头身近似；不再有程序化素体回退，也不复用同一网格缩放冒充体型 | 六个新增 glTF 均已验证 1 skin/23 joints/17 animations、10 个必需人体关节齐全，HTTP 200 可加载且构建通过；当前自动化 Chrome WebGL 读回为黑帧，尚缺六体真实可视截图与 LibTV 视觉并排证据 |
| 空对象 | 已实现无网格实体 | LibTV 辅助图标/轴心像素对比 |
| 人群 3×3 | 已实现固定阵列入口 | 默认间距、命名、层级需与 LibTV 实测 |
| 几何模型 | 立方体、球、圆柱、圆环、圆锥、棱锥已实现 | 默认尺寸/材质/朝向逐项对比 |

## 角色与姿势

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 属性：名称、位置、旋转、缩放、统一缩放、颜色 | 已实现；XYZ 使用 LibTV 可拖动轴按钮，统一缩放固定一位小数，颜色为 `#` + 六位 hex，新增角色默认色对齐 `#4F8EF7`；角色旋转 UI 统一按角度编辑，边界处转换为 Three.js 内部弧度 | 控件尺寸、范围的像素复核 |
| 固定 20 姿势 | 入口顺序和名称已对齐；独立 Playwright/SwiftShader 已把 20 个姿势依次应用到全部八体并保存 20 张 1470×900 证据图（共 160 个模型/姿势组合），站立/T型/行走/跑步/坐蹲跪/叉腰/鞠躬/格斗/踢/投掷/推进/招手/伸手/抱臂/看手机均真实渲染，控制台 0 应用错误 | 多项角度仍基于近似预设而非从 LibTV 骨骼数值逆向取得，需继续逐姿势叠图微调 |
| 身体/躯干/头/肩肘/髋膝调节 | 已驱动骨骼；身体与躯干已拆为两级角色。面板结构改为 LibTV 的身体、躯干、头部、手臂 — 肩、肘部、腿部 — 髋、膝部，并以独立“左/右”小节展示；字段使用前倾/转身/侧倾/扭转/点头/转头/歪头/前举/外展/弯曲/前抬原文及“字段角度”文本框语义 | 构建通过，三套命名体系已静态覆盖；仍需在八体逐项拖动身体/躯干并录取视觉证据 |
| 运动轨迹页 | 入口存在 | 仍需彻底替换旧 motion/path 数据模型 |
| 八体型骨架一致性 | 已同时支持 Universal Base Unreal Humanoid 命名与 Ultimate Animated 的 `UpperArm/LowerArm/UpperLeg/LowerLeg .L/.R` 命名；六个新增模型静态审计均覆盖 body/spine/neck/双肩肘/双髋膝；20×8 批量视觉回归完成且无爆模/解析错误 | 仍需逐体高分辨率近景检查手脚穿插和与 LibTV 的精确体型差异 |

## 摄像机与截图

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 属性、机位切换、位置、跟随、旋转、注视、FOV | 已统一到共享相机姿态解析器：手动坐标、手动旋转、角色注视互斥；跟随保存相对偏移并随角色移动；切换机位同步当前选择；FOV 文案及一位小数格式对齐 | 完整 Modal 回归宿主已执行相机编辑→关闭→页面刷新→重开；位置 1.25/2.2/10 与手动旋转模式恢复，应用错误 0。仍需对 LibTV 做逐模式视觉位姿及跟随运动对比 |
| 机位视角实时预览 | 视锥 helper、机位对象、主机位预览与截图使用同一解析结果；属性修改在渲染帧立即生效，手动旋转不再被 `lookAt()` 覆盖；机位对象同步最终四元数，视锥宽高比同步当前画幅，不再固定 16:9 | Vite/TypeScript 构建通过；仍需真实画布逐控件截图与 LibTV 像素对比 |
| 摄像机截图分组 | 已实现；截图即时显示 dataURL，随后上传远程并仅把远程 URL 写入 `scene.cameraShots`，关闭重开可重新水合画廊；同步写入 `dataRef` 防止快速上传回调从旧 scene 重建并丢失新机位，外部 scene 水合也同步画廊 | 完整 Modal 宿主已验证远程路径截图关闭→刷新→重开后按“机位1截图”分组和原 URL 水合，持久化 JSON 无 data:/blob:；仍需真实账号 OSS 上传与画布输出证据 |
| 单张删除/发送/全屏 | 已实现；全屏改为导演台内遮罩灯箱，支持背景关闭、关闭按钮与 Esc | 本地真实项目已验证截图后出现分组卡片、打开 `dialog`、Esc 关闭；仍需与 LibTV 灯箱视觉逐像素复核 |
| 全部清空/全部发送 | 已实现 | 批量失败回滚和顺序已校验，仍需重载验证 |
| 删除对象的数据清理 | 删除机位同步清理其 `cameraShots`；删除任意对象同步清理属性 tracks 与 trajectory，activeCamera 自动回落；全部发送只读取当前仍存在的机位分组 | 构建通过；删除后保存重载浏览器证据待补 |
| 截图所见即所得 | 场景变换、标签、地面、当前画幅进入截图 | 色彩管理、全景采样、抗锯齿逐像素对比 |

## 底栏菜单

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 移动、添加角色、全景、机位、画幅、截图、全屏、场景/时间线 | 已按 LibTV 改为图标按钮下方常驻文字：移动 (V)、添加角色、全景图、添加机位、选择画幅比例、截图、AI 识图导入、全屏、场景编辑、动画时间轴；工具条使用 navigation 语义 | 完整 Modal 快照逐项出现，与 LibTV 摄像机快照的工具栏文本顺序一致；仍需像素尺寸/间距对比 |
| 添加角色菜单 | 一级菜单对齐本地上传、高斯泼溅、八套素体、添加空对象、群众 (3x3)、几何模型；几何模型改为二级入口，不再把 Tanva 几何清单直接铺在一级菜单 | 文案与层级按 LibTV `director-add-character` 快照修正；生产构建通过，仍需二级菜单线上逐项快照 |
| 全景本地上传 | 已远程持久化 | 历史条目记录/选择回归 |
| 全景历史记录 | 已实现带 requestId/purpose 的图片选择模式；选中历史图片后关闭历史页、回到导演台并把远程 URL 写入 `scene.skybox` | 真实项目已验证“全景背景→历史记录→机位截图图片”后显示“已连接全景图/已应用历史全景图”；仍需刷新重载验证 |
| 全景 AI 生成 | 已接真实 2:1 生图、远程化和应用 | 未在浏览器实际付费生成验证 |
| AI 识图导入 | 已实现本地上传、历史选择回填、插入/覆盖、参考生图、远程图片节点与 `img→target` 替换连边、3D 站位参考层 | 真实项目已验证历史页选择图片后回到对话框、显示“识图来源/已选择历史图片”；尚未实际付费生成验证 |

## 动画时间线

| 功能 | 状态 | 未完成项 |
|---|---|---|
| 播放、自动关键帧、循环、播放头、总时长、秒/毫秒 | 已按 LibTV 控件语义改为“播放”“自动帧”“循环播放”、`播放头位置`/`总时长` 文本框及 `s`/`ms` 单按钮切换 | 构建通过；仍需浏览器输入草稿/边界值回归 |
| 时间线最小化/展开 | 已实现真实 250px ↔ 42px 收起状态，按钮 aria/图标同步变化 | 需与 LibTV 收起后的精确高度和过渡动画对比 |
| 轨道增删、缩放、属性展开 | 已实现真实数据行为；轨道管理收敛为单个上下文按钮“新建轨道/移除轨道”，缩放改为 LibTV range，未建轨角色不再提前出现在列表；属性展开是独立按钮 | 轨道排序交互复核 |
| 角色位置/旋转/缩放/统一缩放/姿势轨 | 已实现；位置/旋转/缩放升级为逐 X/Y/Z component 轨，时间线模式右侧每个轴均显示“当前帧有/无关键帧”菱形按钮；旧整 Vec3 轨继续兼容采样/删除。永久回归门禁 `npm run verify:director-timeline` 已通过，覆盖三轴建轨、空轨初态、单轴隔离及新旧轨优先级 | 姿势插值模式需与 LibTV 实测；逐轴浏览器回归待补 |
| 旋转最短弧插值 | 角色 rotation 按 2π 弧度周期、摄像机 rotation 按 360° 周期、逐关节 pose 按 2π 周期插值，避免 350°→10° 或跨 ±π 时绕长路 | 永久门禁通过真实模块 esbuild 后的 Node 断言：摄像机 Y 分量 350°→10° 的 5s 中点为 360°；仍需时间线跨界播放视觉证据 |
| 主机位位置/旋转/FOV/注视轨 | 已实现；位置/旋转/注视点为逐轴 component 轨，FOV 为标量轨，右侧属性面板提供独立关键帧菱形按钮；名称对齐“主机位”，绘制轨迹在主行常驻 | 主机位切换语义复核；逐轴浏览器回归待补 |
| 绘制轨迹 | 已为角色和主摄像机接入同一 LibTV-style 视口编辑器：从对象当前位置起步、点击地面加点、拖动点、删除选中点、清空、折线/曲线切换、完成；控制路径持久化在属性时间线，折线按弧长分配关键帧时间，曲线经 Catmull–Rom 等弧长采样生成 position 关键帧；角色 Y 走地面/高斯吸附，摄像机保持高度 | 真实项目已验证角色/主摄像机均显示绘制入口、角色落点后 position 轨出现关键帧、折线切曲线、关闭刷新重开后关键帧仍存在；仍需真实拖动控制点与播放轨迹的视觉录屏对照 |
| 导出到画布 | 已实现属性时间线主机位实时录制，优先 H.264/MP4（avc3），上传远程 URL 后通过既有 `canvas:insert-video` 写入画板视频素材；不恢复旧 shot/分段/视频节点流程 | 本地真实项目已完成 10s 录制、上传、`video-inserted` 与 Paper 自动保存；浏览器编码探测及 700ms 独立实录产出 85,041 bytes；仍需刷新后视频播放验证与 LibTV 导出参数逐项对比 |
| 顶栏“导出到画布”状态 | 仅动画时间轴模式显示；场景编辑模式隐藏，与 LibTV 默认导演台及时间线快照一致 | 完整 Modal 浏览器快照已验证：场景模式无该按钮，切换动画时间轴后出现 |

## 必须删除的 Tanva 遗留

- 用户可见入口已移除：灰模 MP4、镜头片段、焦距/景深、飞行录制、环绕、动作库、群演广播等。
- `DirectorConsoleModal` 已物理删除灰模 MP4 编码、视频上传/节点输出、15 秒拆分、旧 shot timeline、环绕预览、飞行录制与群演属性广播执行链。
- `DirectorCaptureRunner` 已删除隐藏的 Agent `clip` 灰模视频执行分支；`sendToCanvas` 现在只允许输出 LibTV 摄像机截图图片。
- 已删除旧 `CameraPropertiesPanel`、导演台视频上传器、飞行录制和旧时间线逐帧渲染文件。
- 已物理删除 `Viewport` 的 `captureClipFrames`/`captureFrameAt`、旧 `previewAnim`、自由飞行录制、蓝色录制轨迹、橙色 cameraPath、liveCamera override 和对应混合器注册；同时删除旧 `timeline/previewClip/clipAnimation/cameraPath/cameraPathEdit/lens/shots` 文件及 CameraObj 焦距/光圈/景深/roll/path 类型。当前 Viewport 只保留 LibTV 截图、属性时间线相机姿态和新的轨迹编辑器。

## 当前结论

状态：**未完成**。摄像机、截图灯箱、历史回填、属性时间线动画导出和角色/主机位轨迹已在真实项目验证，旧 Viewport 执行链已收口；逐轴属性时间线已有可重复的永久回归门禁且生产构建通过。高斯地面尚缺真实项目上传/实时拖动/保存重载证据；当前最大硬缺口是八体姿势与 LibTV 精确角度对比、真实 AI 生成验证、完整输入输出保存重载，以及像素级 UI/截图对照。
