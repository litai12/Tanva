# 实施任务

- [x] 安装 Tencent BrowserSkill，并以 `bsk doctor` 验证 CLI、daemon、扩展和协议。
- [x] 使用用户现有登录态进入 Liblib 导演工作区，完成首轮无障碍树与交互结构取证。
- [x] 补齐目标导演台节点编辑态、场景树、属性栏、视角切换与底部工具条取证；工作区截图受 BrowserSkill 3D 页截图超时影响，已用 snapshot + HTML 样式证据替代。
- [ ] 审计 Tanva FlowOverlay、Canvas 工具列、节点添加面板及 directorConsole 的数据与事件边界。
- [ ] 提取导演工作区 shell 和视觉 token，完成默认画布壳层改版。
- [ ] 对齐顶部导航、工作流/故事板切换、同步/协作/Agent 区域。
- [ ] 对齐左侧工具列和添加节点面板的信息架构与交互。
- [x] 对齐导演台编辑态的双视角、帮助/关闭、默认机位/角色、属性栏与场景/时间线模式，保持旧 scene/timeline 数据兼容。
- [x] 根据用户修正范围：取消“保留旧能力”的兼容目标，以 LibTV 为唯一规格；完成 3D 场景面板第一阶段与 `target/source` 句柄迁移。
- [x] 补齐 `3D场景` 环境控制入口：点击视口空白可取消对象选择；地面板/网格的显示、透明度、高度同时进入视口和截图；角色/机位网格吸附真实生效，并按在线状态同步高斯地面吸附默认开启。高斯点云表面求交吸附仍待实现。
- [x] 将场景缩放/平移/旋转、天空颜色/球半径、地面和角色标签接入实时渲染与摄像机截图；`target` 输入按上游当前裁剪结果生成仅运行时 object URL，不持久化内联图片。
- [ ] 删除灰模样片、镜头预设、景深元数据、路径/飞行运镜、群演广播、自定义动作库等 LibTV 不存在的 UI 与执行路径；用户可见入口已移除，旧实现文件/模态内部执行代码仍待物理删除。
- [x] 摄像机 UI 已替换为 LibTV 字段集，旧景别/焦距/景深/路径/飞行/环绕/灰模入口不再渲染；角色与时间轴旧入口仍待替换。
- [ ] 将镜头片段时间线替换为 LibTV 属性关键帧轨道；角色/主摄像机逐属性关键帧的添加、删除、持久化、自动打帧、轨道增删、循环播放和线性插值已实现，角色姿势/逐关节欧拉角也会逐关节插值；仍需对照 LibTV 核实姿势轨采用连续插值还是离散阶梯，以及是否存在缓动曲线。
- [x] 角色栏移除动画/自定义/收藏/群演广播入口，按 LibTV 固定顺序展示 20 个姿势及身体、躯干、头部、肩肘、髋膝调节；时间线模式显示运动轨迹页签。
- [ ] 截图输出已完成真实浏览器首轮闭环：WebGL 截图约 45KB，上传得到可 HTTP 200 访问的远程 JPEG，Flow DOM 出现 `image` 节点 `img_1784381763834` 和 `Edge from directorConsole_1784378288903 to img_1784381763834`；发送函数现强校验远程引用和节点数量。测试输出节点已清理，但测试项目随后水合长期停在“加载中”，仍需在可稳定保存的干净项目验证重载持久化。
- [x] 修复导演台模态内 Delete/Backspace 穿透到底层 Flow 快捷键导致导演台节点被同时删除：对象编辑键盘监听改为捕获阶段并阻止后续同键处理。
- [x] 审计当前素体资产：确认八个菜单项全部复用 `xbot.glb`，仅高度/宽度缩放不同，不满足 1:1。
- [ ] 用 BrowserSkill 逐个创建并采集 LibTV 八套素体的正面/侧面/默认透视、属性默认值、姿势和截图表现，完成后清理测试对象。
- [x] 修复对象菜单基础行为：空对象使用无网格实体，几何模型补齐圆环/棱锥；本地模型、全景和 `.splat` 高斯泼溅均先上传为远程 URL 再写 scene，禁止把运行时 `blob:` 持久化。
- [ ] AI 识图导入已完成弹窗、来源上传、远程图片节点、替换 `target` 连边、插入/覆盖模式、参考生图和 3D 站位参考层代码，并由 BrowserSkill 验证全部控件可见；仍需实际执行一次付费参考生图并验证历史选择回传、保存和重载。
- [ ] 为八套素体引入独立许可兼容资产，登记来源/许可证，并删除 `widthScale` 伪体型方案。
- [ ] 建立八套骨架重定向与姿势回归样例，验证 20 个 LibTV 姿势预设和关节调节。
- [ ] 对齐素材库、角色库、历史记录、资产管理入口及其展开面板。
- [ ] 对齐小地图、连线、网格吸附和缩放控制。
- [ ] 添加固定示例数据与自动化测试；已用本地 BrowserSkill 完成双视角、角色属性、20 个姿势、场景/时间线的交互验收，确认无 Invalid hook call/白屏且旧添加镜头/合成视频/灰模文本不再出现。
- [ ] 前端生产 build 已通过两次，改动文件定向 lint 已通过（`DirectorConsoleModal.tsx` 仍有既存 ts-nocheck/any 基线）；全仓 lint 被 2520 个存量错误阻断。待完成最终设计 JSON 审计。
- [ ] 同步 helloagents wiki、CHANGELOG 和相关 frontend/docs。
